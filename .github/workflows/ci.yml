name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    name: Test
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore
        run: dotnet restore Sims3ModernPatcher.sln

      - name: Build
        run: dotnet build Sims3ModernPatcher.sln -c Release --no-restore

      - name: Test
        run: dotnet test Sims3ModernPatcher.sln -c Release --no-build --verbosity normal
