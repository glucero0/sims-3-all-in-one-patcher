name: CodeQL

on:
  pull_request:
  push:
    branches:
      - main
  schedule:
    - cron: "37 4 * * 1"

permissions:
  contents: read

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    # WPF / net8.0-windows cannot autobuild on Linux. Trace a single
    # Windows compile of the app project so CodeQL can extract C#.
    runs-on: windows-latest
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: csharp
            build-mode: manual
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}

      - name: Restore
        run: dotnet restore Sims3ModernPatcher.sln

      # Build the app project once. A solution build also compiles it through
      # the test ProjectReference (SkipAppManifest=true), and CodeQL's tracer
      # keeps WPF-generated App.g.cs locked during that second pass.
      - name: Build
        env:
          MSBUILDDISABLENODEREUSE: 1
        run: dotnet build Sims3ModernPatcher.csproj -c Release --no-restore -m:1 -p:UseSharedCompilation=false

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:${{matrix.language}}"
